# מדריך התקנה — בוט Webtop לטלגרם

מדריך צעד-אחר-צעד להתקנת הבוט על מחשב Windows.
כל המידע נשאר אצלך במחשב. שום סיסמה לא נשלחת לשום מקום חוץ מאתר בית הספר עצמו.

זמן משוער: 15–20 דקות.

---

## מה צריך לפני שמתחילים

- מחשב Windows (עובד גם על Linux ו-macOS, הפקודות מעט שונות)
- חשבון Webtop / סמארטסקול — הורה או תלמיד
- חשבון טלגרם

---

## שלב 1 — התקנת Python

1. היכנס ל-<https://www.python.org/downloads/> ולחץ על הכפתור הגדול להורדה.
2. הרץ את הקובץ שירד.
3. **חשוב מאוד:** במסך הראשון של ההתקנה סמן את התיבה
   **Add python.exe to PATH** (למטה, לפני שלוחצים Install).
   בלי זה הפקודות בהמשך לא יעבדו.
4. לחץ **Install Now** וחכה לסיום.

לבדיקה שההתקנה הצליחה — פתח חלון **PowerShell** (כפתור התחל, כתוב `powershell`,
Enter) והקלד:

```
python --version
```

אמור להופיע מספר גרסה, למשל `Python 3.12.4`. אם מופיעה שגיאה — התקן שוב וודא
שסימנת את התיבה של PATH.

---

## שלב 2 — הורדת הקוד

בדף ה-GitHub של הפרויקט לחץ על הכפתור הירוק **Code** ואז
**Download ZIP**.

חלץ את ה-ZIP לתיקייה קבועה — למשל `C:\webtop-bot`.
אל תשאיר אותו בתוך תיקיית ההורדות; הבוט ירוץ מהמקום הזה כל הזמן.

---

## שלב 3 — התקנת הספריות

פתח PowerShell **בתוך התיקייה** של הפרויקט. הדרך הקלה: פתח את התיקייה
בסייר הקבצים, לחץ על שורת הכתובת למעלה, כתוב `powershell` ולחץ Enter.

הרץ:

```
pip install -r requirements.txt
```

זה לוקח דקה או שתיים. בסוף אמורה להופיע שורת `Successfully installed`.

---

## שלב 4 — יצירת בוט בטלגרם

1. פתח טלגרם וחפש את **@BotFather** (עם הסימון הכחול).
2. שלח לו `/newbot`.
3. הוא יבקש שם לבוט — כתוב מה שבא לך, למשל `הבוט של המערכת`.
4. הוא יבקש שם משתמש — חייב להסתיים ב-`bot`, למשל `mySchoolHelperBot`.
   אם השם תפוס הוא יבקש אחר.
5. בסוף הוא שולח הודעה עם קוד ארוך, בערך כזה:
   `8123456789:AAH1a2B3c4D5e6...`

**זה הטוקן שלך. שמור אותו — נשתמש בו בשלב הבא.**
אל תשלח אותו לאף אחד; מי שיש לו את הטוקן שולט בבוט.

---

## שלב 5 — קובץ ההגדרות

כל הפרטים שלך נמצאים בקובץ אחד, במחשב שלך בלבד, בשם `.env`.

בחלון PowerShell שפתחת בשלב 3, הרץ:

```
copy .env.example .env
```

עכשיו פתח את הקובץ `.env` בעורך טקסט. אם אין לך אחד, Notepad מספיק:

```
notepad .env
```

תראה שורה לכל הגדרה. כתוב את הערך שלך **מיד אחרי סימן ה-`=`**, בלי מרכאות
ובלי רווחים סביב הסימן.

| השורה בקובץ | מה לכתוב שם |
|---|---|
| `TELEGRAM_TOKEN=` | הטוקן הארוך שקיבלת מ-BotFather בשלב 4 |
| `WEBTOP_USERNAME=` | שם המשתמש שלך ב-Webtop — בדיוק כמו שאתה מקליד באתר |
| `WEBTOP_PASSWORD=` | הסיסמה שלך ב-Webtop |
| `ALLOWED_CHAT_IDS=` | להשאיר ריק בינתיים — נמלא בשלב 7 |
| `WEBTOP_DATA=` | להשאיר ריק — רק אם ההתחברות נכשלת (ראה בהמשך) |
| `WEBTOP_BASE_URL=` | להשאיר ריק — רק לבתי ספר על שרת לא סטנדרטי |

הקובץ המוכן נראה בערך כך:

```
TELEGRAM_TOKEN=8123456789:AAH1a2B3c4D5e6F7g8H9i0J1k2L3m4N5o6P
WEBTOP_USERNAME=my_username
WEBTOP_PASSWORD=my_password
WEBTOP_DATA=
WEBTOP_BASE_URL=
ALLOWED_CHAT_IDS=
```

שמור וסגור.

> **שים לב ב-Windows:** Notepad נוטה לשמור את הקובץ בשם `.env.txt` והבוט
> לא ימצא אותו. בחלון השמירה שנה את *Save as type* ל-**All Files**.

---

## שלב 6 — הפעלה ראשונה

באותו חלון PowerShell:

```
python bot.py
```

השאר את החלון פתוח. אם שם המשתמש והסיסמה נכונים, יופיע `Login OK` עם שם
התלמיד.

> **אם ההתחברות נכשלת** עם הודעה על פרטי התחברות — יכול להיות שבית הספר שלך
> דורש מפתח נוסף. פתח את אתר Webtop בדפדפן, לחץ `F12` ← לשונית **Network** ←
> התחבר רגיל ← לחץ על הבקשה בשם `LoginByUserNameAndPassword` ← העתק את הערך
> של `data` מגוף הבקשה אל תוך `WEBTOP_DATA=` בקובץ `.env`.

---

## שלב 7 — נעילת הבוט אליך בלבד

כרגע הבוט לא מוכן לשימוש: הוא חוסם **כל** פקודה חוץ מ-`/myid`, כי הוא עדיין
לא יודע מי אתה. זה מכוון.

1. בזמן שהבוט רץ, פתח את הבוט שלך בטלגרם ושלח לו `/myid`.
2. הוא יענה עם מספר, למשל `123456789`.
3. פתח שוב את `.env`, כתוב את המספר אחרי `ALLOWED_CHAT_IDS=` ושמור.
4. עצור את הבוט בחלון PowerShell עם `Ctrl+C`, והפעל שוב:

```
python bot.py
```

עכשיו שלח `/start` בטלגרם — אמור להיפתח התפריט הראשי.

**אל תדלג על השלב הזה.** בלעדיו, כל מי שימצא את הבוט יוכל לקרוא את המידע
של התלמיד.

אפשר להוסיף עוד אנשים (למשל שני ההורים) עם פסיק בין המספרים:

```
ALLOWED_CHAT_IDS=123456789,987654321
```

---

## שלב 8 — הפעלה אוטומטית ברקע (אופציונלי)

כדי שהבוט ירוץ תמיד, בלי חלון פתוח, ויעלה לבד בכל הדלקה של המחשב:

```
.\setup_autostart.ps1
```

אם PowerShell מסרב להריץ סקריפטים, הרץ פעם אחת:

```
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

ואז נסה שוב.

לאחר מכן:

- **להפעיל מחדש** — הרץ `restart-webtop-admin.bat` (לחיצה כפולה)
- **לראות לוגים** — הקובץ `logs\bot.log` בתוך תיקיית הפרויקט
- **לעצור** — `Stop-ScheduledTask -TaskName 'WebtopBot'`

---

## שלב 9 — התאמת שעות הצלצול לבית הספר שלך

לכל בית ספר שעות אחרות. השעות נמצאות בקובץ `schedule_times.py`, והן
**ברירת מחדל שצריך לערוך**:

- `HOUR_TIMES` — שעת התחלה וסיום לכל שיעור
- `BREAKS_AFTER` — ההפסקות, ואחרי איזה שיעור הן באות
- `FRIDAY_LAST_HOUR` ו-`FRIDAY_ENDS_AT` — יום שישי הקצר.
  אם בבית הספר שלך שישי הוא יום רגיל, שנה את `FRIDAY_ENDS_AT` ל-`""`

ערוך, שמור, והפעל את הבוט מחדש.

---

## שלב 10 — מערכת שעות ידנית (רק אם צריך)

הבוט מושך את המערכת מ-Webtop לבד. אבל בין שנות לימוד, ובשבועות הראשונים
של שנה חדשה, Webtop לפעמים חוסם את התצוגה או מחזיר מידע שגוי.

במקרה כזה אפשר להקליד את המערכת מהדף המודפס בקובץ `schedule_overrides.py`.
בתוך `OVERRIDES` יש דוגמה מוערת (עם `#`) — הסר את סימני ההערה ומלא:

```python
OVERRIDES = {
    2: {                                   # יום שני
        1: ("מתמטיקה", "", "כפל וחילוק"),  # (מקצוע, מורה, נושא)
        2: ("עברית", ""),                  # (מקצוע, מורה)
    },
}
```

מספרי הימים: 1=ראשון, 2=שני, 3=שלישי, 4=רביעי, 5=חמישי, 6=שישי.

יום שמופיע כאן מוגדר **במלואו** — שיעור שה-API מחזיר ליום הזה אבל לא רשום
כאן, יימחק. כך הדף המודפס גובר גם כשה-API חוזר לעבוד.

אם לא צריך — פשוט אל תיגע בקובץ. הבוט יעבוד מול ה-API בלבד.

---

## מה הבוט יודע לעשות

| כפתור / פקודה | מה זה עושה |
|---|---|
| 🗓️ מערכת שעות | מערכת יומית ושבועית, כולל תמונה |
| 📚 שיעורי בית | דפדוף יום-יום, עובר בין שבועות לבד |
| 📊 ציונים | רשימת הציונים |
| 📋 משמעת | אירועי משמעת |
| 📬 הודעות | הודעות מבית הספר, חודש-חודש |
| `/summary` | סיכום היום, עכשיו |
| `/testschedule` | המערכת של מחר, עכשיו |
| `/myid` | הצגת ה-chat ID (להתקנה) |
| `/clear` | מחיקת ההודעות של הבוט מהצ'אט |
| `/0` | התחברות מחדש ל-Webtop |

**אוטומטית:** בדיקת חידושים כל 10 דקות, סיכום יומי ב-17:00, ומערכת של מחר
ב-19:00.

**מה עדיין לא קיים:** אין ניהול חופשות וחגים. הבוט מציג את מה ש-Webtop
מחזיר; ביום חופש שבית הספר סימן במערכת, היום פשוט יוצא ריק. אין לוח חופשות
מובנה ואין השתקה של ההתראות בחופשים.

---

## פתרון תקלות

**`python` לא מזוהה כפקודה** — לא סומנה התיבה *Add python.exe to PATH*
בהתקנה. התקן את Python מחדש עם התיבה מסומנת.

**`TELEGRAM_TOKEN is not set in .env`** — הקובץ נשמר בשם `.env.txt`,
או שהוא לא בתיקייה של `bot.py`. בדוק את שם הקובץ המדויק.

**הבוט לא עונה לשום דבר חוץ מ-`/myid`** — לא הושלם שלב 7, או שהבוט לא הופעל
מחדש אחרי עריכת `ALLOWED_CHAT_IDS`.

**"אין הרשאה"** — ה-chat ID בקובץ לא תואם. שלח `/myid` שוב והשווה ספרה-ספרה.

**שגיאת התחברות ל-Webtop** — ראה את ההערה על `WEBTOP_DATA` בשלב 6.

---

## אבטחה — מה חשוב לדעת

- **הבוט לעולם לא מבקש סיסמה בטלגרם.** הסיסמה נקראת פעם אחת מהקובץ `.env`
  במחשב שלך. אם משהו שמתחזה לבוט הזה מבקש ממך להקליד סיסמה בצ'אט — זה לא
  הבוט הזה.
- הקבצים `.env`, `.device_id`, `logs/` ו-`notification_state.json` מוחרגים
  מ-git. **אף פעם אל תעלה אותם לאינטרנט** — הם מכילים את הסיסמאות שלך ואת
  המידע האמיתי של התלמיד.
- אם אתה משתף את העותק שלך — הרץ `git status` וודא שאף אחד מהם לא נכלל.
- ההרשאות סגורות כברירת מחדל: `ALLOWED_CHAT_IDS` ריק חוסם את כולם, לא מתיר
  את כולם.
